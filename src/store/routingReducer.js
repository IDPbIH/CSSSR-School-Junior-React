// Routing Module.js

// Actions


//initialState
const initialState = {
    
};

// Reducer
const routingReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

// Action Creators

// Selectors

export default routingReducer;